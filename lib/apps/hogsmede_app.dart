import 'package:flutter/material.dart';

class HogsmedeApp extends StatelessWidget {
  const HogsmedeApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(theme: ThemeData.dark());
  }
}
